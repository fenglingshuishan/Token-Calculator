services:
  token-calculator:
    build:
      context: .
    image: token-calculator:3.0.0-archive
    ports:
      - "8000:8000"
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp:size=64m
    security_opt:
      - no-new-privileges:true
